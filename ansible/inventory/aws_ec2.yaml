plugin: amazon.aws.aws_ec2
regions:
  - ap-northeast-1
filters:
  tag:Project: BinanceFix
  instance-state-name: running
keyed_groups:
  - key: tags.Name
    prefix: tag
compose:
  ansible_host: public_ip_address